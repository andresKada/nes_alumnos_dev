<table>
  <tr>
    <td><b>Nombre de Usuario:</b></td>
    <td><%= form.text_field :login %></td>
  </tr>
  <tr>
    <td><b>Correo Electrónico:</b></td>
    <td><%= form.text_field :email %></td>
  </tr>
  <tr>
    <td><b>Rol:</b></td>
    <td>
      <% for role in Role.all %>
        <% checked = ""  %>
        <% checked = "checked" if @role.to_i == role.id %>
        <input type="radio" id="role_ids" name="role_ids" value="<%= role.id %>" <%= checked %>><%= role.name %></input>
        <br/>
      <% end%>
    </td>
  </tr>
  <tr>
    <td><b>Campus:</b></td>
    <td>
      <%= form.select :campus_id, Campus.all.map{|campus| [campus.nombre,campus.id]} %>
    </td>
  </tr>
  <tr>
    <td><b>Contraseña:</b></td>
    <td><%= form.password_field :password %></td>
  </tr>
  <tr>
    <td><b>Confirmación de Contraseña:</b></td>
    <td><%= form.password_field :password_confirmation %></td>
  </tr>
</table>

<script type="text/javascript">
  document.getElementById("user_full_name").focus();
</script>